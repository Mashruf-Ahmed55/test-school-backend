<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Digital Competency Certificate</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 2px solid #1a5276;
    }
    .logo {
      max-width: 150px;
      height: auto;
    }
    .certificate-preview {
      background-color: #f9f9f9;
      border: 1px solid #ddd;
      padding: 20px;
      margin: 20px 0;
      text-align: center;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #1a5276;
      color: white !important;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
      margin: 10px 0;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      font-size: 12px;
      color: #777;
      text-align: center;
    }
    .signature {
      margin-top: 30px;
      font-style: italic;
      color: #555;
    }
  </style>
</head>
<body>
  <div class="header">
    <% if (appLogo) { %>
      <img src="<%= appLogo %>" alt="<%= appName %> Logo" class="logo">
    <% } %>
    <h1><%= appName %></h1>
  </div>

  <h2>Congratulations, <%= userName %>!</h2>

  <p>You have successfully earned your <strong><%= certificationLevel %> Digital Competency Certificate</strong>.</p>

  <div class="certificate-preview">
    <h3><%= certificationLevel %> Digital Competency Certificate</h3>
    <p>Awarded to: <strong><%= userName %></strong></p>
    <p>Certificate ID: <code><%= certificateId %></code></p>
    <p>Issued on: <%= issuedDate %></p>
    <img src="https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=<%= verificationUrl %>" alt="Verification QR Code">
  </div>

  <div style="text-align: center;">
    <a href="<%= downloadUrl %>" class="button">Download Your Certificate</a>
  </div>

  <div class="signature">
    <p>Best regards,</p>
    <p>The <%= appName %> Team</p>
  </div>

  <div class="footer">
    <p>© <%= currentYear %> <%= appName %>. All rights reserved.</p>
    <p>This certificate was issued digitally and does not require a signature.</p>
    <p>Need help? Contact us at <%= supportEmail %></p>
  </div>
</body>
</html>
